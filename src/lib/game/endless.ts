import type { EndlessStrategy } from '$lib/types';

// TODO phase 2: endless mode starts after story completion.
// This placeholder keeps imports stable before the actual procedural strategy.
export const endlessStrategy: EndlessStrategy = {
  next() {
    return null;
  },
};
